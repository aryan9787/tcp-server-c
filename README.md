# TCP Server in C

A simple TCP server built using sockets in C.

## Features
- Handles client connections
- Reads and writes data
- Built on Linux (WSL)

## How to run

```bash
gcc srv.c -o srv
./srv
